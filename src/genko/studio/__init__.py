"""Agent-facing tools: validate, compile and render what an external agent writes.

Genko does not call any LLM or image model. Agents (e.g. Hermes Agent over MCP)
send bibles, scripts and name plans; this package checks and compiles them.
"""
